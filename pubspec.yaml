name: truce
description: "A tracking application for price movements in Egypt."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.6.0
  flutter_bloc: ^8.1.6
  get_it: ^7.7.0
  intl: ^0.19.0
  google_sign_in: ^6.2.1
  http: ^1.1.0
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
